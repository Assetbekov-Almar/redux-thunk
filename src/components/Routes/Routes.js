import { Route, Routes as Routing } from "react-router-dom"
import {MAIN_URL} from "./RoutesPath";

const Routes = () => {
  return (
    <Routing>
      <Route path={MAIN_URL} component={}/>
    </Routing>
  )

}

export default Routes